{
  "name": "revenue-leakage-detective",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "uvicorn backend.app.main:app --reload",
    "install:all": "npm install && npm install --prefix frontend && pip install -r requirements.txt"
  }
}
